@{
    
    Layout = null;
}
@model webProjectMVC.Models.Sign


    <div>
        @*<form class="sign-up">*@
        @using (Html.BeginForm("SignUpUser", "SignUp", FormMethod.Post))
        {
            <h1 class="sign-up-title">Sign up </h1>
            @*<input type="text" class="sign-up-input" placeholder="What's your username?" autofocus>
                <input type="password" class="sign-up-input" placeholder="Choose a password">*@

            @Html.TextBoxFor(m => m.UserName, new { placeholder = "Name" })
            <br>
            @Html.TextBoxFor(x => x.Password, new { placeholder = "Password" })
            <br>
            <input type="submit" value="Sign me up!" class="sign-up-button">
        }

        @*</form>*@
    </div>
