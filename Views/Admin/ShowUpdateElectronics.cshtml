@model webProjectMVC.Models.Electronics__
@{
    ViewBag.Title = "ShowUpdateElectronics";
}
@{
    Layout = null;
}
<h2>ShowUpdateElectronics</h2>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ShowUpdate</title>
</head>
<body>
    <div>

        <div class="container">
            <div class="row">
                @using (Html.BeginForm("SaveElectronics", "Admin", FormMethod.Post))
                {
                    <div class="col-md-3">

                        @*<img src="~/MobileImage/@Model" />*@

                    </div>
                    <div class="col-md-3">

                        <h4> Name:</h4>  <input type="text" value="@Model.Name" name="Name" /><br />
                        Price:<input type="text" value="@Model.Price" name="Price" /><br />
                       <input type="text" style="display:none" value="@Model.Id" name="Id">
                        <button type="submit" class="btn btn-primary">Update</button>

                    </div>
                }
            </div>
        </div>

    </div>
</body>
</html>
